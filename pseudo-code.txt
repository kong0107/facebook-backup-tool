for each query to `/me/posts` or `/me/feed`
	for each page of the query result
		for each post
			get `application`'s id
			get `place`'s id
			get every `with_tags` id
			get every `comments` id
			get every (id, offset, length) of `message_tags`
			get every (id, offset, length) of `story_tags`
		end for		
	end for //each page of the query result
end for //each query to `/me/posts` or `/me/feed`



[[get application]]
get coloumns and return id.

[[get place]]
[[get page]]
get columns and return id.


[[get ALL comments]]
result = [];
for each query
	for each comment
		get columns
		get id of `from`
		get message_tags
	end for // each comment
end for //each query


[[get user|page]]


